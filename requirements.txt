flask>=2.3.0
ffmpeg-python>=0.2.0
python-dotenv>=1.0.0
werkzeug>=2.3.0

# Image processing
Pillow>=10.0.0

# ML/AI packages (OPTIONAL - comment out if installation fails on cPanel)
# These enable AI-enhanced compression but are not required
# opencv-python-headless>=4.8.0
# numpy>=1.24.0

# Production WSGI server (optional - for non-Passenger deployments)
# gunicorn>=21.0.0